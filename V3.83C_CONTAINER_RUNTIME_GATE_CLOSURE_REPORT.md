# V3.83-C Container Runtime Gate Closure Report

Date: 2026-08-28 (Asia/Shanghai)

## 1. Status

`DEPLOYMENT_FAILURE_MODE_VALIDATION_READY`

All required Docker Compose image and container gates passed with real Docker Server execution. Provider generation remains optional-unvalidated and remote CI remains external-pending; neither blocks the frozen internal READY gate.

## 2. Git

- Recovery branch: `main`
- Recovery HEAD: `cce8e3432207e86d2de68977c0890a45160a5ae2`
- Recovery tree: clean
- Deployment fix: `f0134f5dff396ecce50dc641ee821d5f0aed980f`
- Push: NO
- RAG correctness code: unchanged

## 3. Docker Daemon

Docker daemon was available with Docker Desktop 4.85.0, Client/Engine 29.6.2, API 1.55, Linux/amd64 Server, and Compose 5.3.1. Both `docker version` and `docker info` exited 0 with real Server information.

## 4. Validation Input Manifest

`V383C_CONTAINER_VALIDATION_INPUT_MANIFEST.json` was read as the immutable entry snapshot. Its source HEAD and Dockerfile/Compose/.dockerignore/runtime/protected-manifest hashes all matched the recovery state. Validation then found one deployment-only image-content defect and pinned its final `.dockerignore` overlay at SHA256 `a59b5444...c3879` in the RC manifest.

## 5. Compose Config

`docker compose config --quiet` passed before execution and again after validation. `COMPOSE_CONFIG_VALID = TRUE`.

## 6. Image Build

The formal command was `docker compose build`. The cold build completed in 127.520 seconds. After the content-boundary fix, the cached rebuild completed in 5.446 seconds with exit code 0. `IMAGE_BUILD_VALID = TRUE`.

## 7. Image Metadata

- Tag: `industrial-knowledge-rag-api:latest`
- Image ID/digest: `sha256:183cc4017252d50ecfdf49f946d7e52cff5d4047161b9ce3c11e38be741bcc99`
- Size: 162,451,362 bytes
- Final application tree: 77 files / 938,878 bytes

## 8. Image Content Audit

The first audit found that `COPY backend` included root `backend/test*.py` files and the offline `backend/evaluation` tree. One test contained a developer-specific absolute path to the ignored private benchmark; the evaluation tree also carried private-evaluation instructions and fixtures not required at runtime.

The minimal `.dockerignore` fix excludes `backend/test*.py` and `backend/evaluation`. After rebuild, actual image filesystem inspection found no tests/evaluation tree, private PDFs, benchmark-private data/results, gold annotations, `.env`, keys/tokens, vector DB, `.git`, private logs, prohibited extensions, local temp state, sentinels, or developer-specific absolute paths. `IMAGE_CONTENT_AUDIT_VALID = TRUE`.

## 9. Runtime User

Image metadata declares `USER app`; actual execution returned `uid=100(app) gid=101(app)`. The container is non-root. Compose set production runtime, debug false, and JSON logs. Error and privacy smoke found no raw traceback or content logging.

## 10. Container Startup

Formal `docker compose up -d` created the project network, four named volumes, and the API container with port 8000 published. Command completion took 1.981 seconds. The first container ID was `758e3c5e...b4a41b`; the final recreated validation container was `460aa87e...667683`. Initial measurement began after startup, so its 21.573-second live observation is only an upper bound; subsequent measured restarts/recreation reached live in 1.27–1.73 seconds.

## 11. Liveness

Real HTTP `/live` returned 200, `status=ok`, and `runtime_mode=light`. Before any index was present, `/live` remained 200 while `/ready` correctly returned 503, confirming process-only liveness and no false-ready behavior.

## 12. Readiness

Before the synthetic index, `/ready` returned HTTP 503 `unavailable`, `DEPENDENCY_UNAVAILABLE`, and `index_index_exists=missing_file`. After the controlled index was installed, the formal keyless container returned HTTP 200 `degraded`: all required mode/index checks passed; `llm_provider` was the only failed optional check with `no_provider_configured`; task worker was healthy. `CONTAINER_HEALTH_VALID = TRUE`.

## 13. Public Smoke

Real container requests passed `/live`, `/ready`, safe ABSTAIN (`Explain bananas in outer space.` -> HTTP 200, refused, zero sources), validation error, dependency error, and `/metrics`. No private query or PDF was used.

## 14. Request ID

Caller request IDs round-tripped through live, ABSTAIN, validation-error, dependency-error, and metrics traffic. Error header/body IDs matched, and all expected IDs appeared in structured container logs without cross-request substitution.

## 15. Structured Logging

JSON logging was active in the container; the formal smoke observed 69 structured JSON log lines and correlated the normal/error/dependency request IDs. Uvicorn recorded complete lifespan shutdown on all three explicit stop cycles.

## 16. Privacy Sentinel

`SECRET_SENTINEL_V383C`, `QUERY_SENTINEL_V383C`, and `PATH_SENTINEL_V383C` were sent through a controlled invalid request. None appeared in response content, metrics, or `docker compose logs`. Final cumulative log scan also found no sentinel. `CONTAINER_PRIVACY_VALID = TRUE`.

## 17. Metrics

Formal container smoke observed real increases: HTTP requests 5->10, request errors 0->2, latency count 5->10, answer-decision counter 0->1, ABSTAIN counter 0->1, and dependency failures 0->1. The successful generation/ANSWER branch remains `OPTIONAL_PROVIDER_CAPABILITY_NOT_VALIDATED`; it was not faked.

## 18. Restart ×3

`docker compose restart api` passed 3/3. Every cycle returned `/live` 200, `/ready` 200 degraded, safe ABSTAIN 200, and the unchanged index digest. There was no crash loop, port conflict, manual cleanup, volume loss, or index mutation. `CONTAINER_RESTART_VALID = TRUE`.

## 19. Restart Timing

| Cycle | Restart -> live | Restart -> ready |
|---:|---:|---:|
| 1 | 1497.32 ms | 1501.93 ms |
| 2 | 1425.72 ms | 1430.43 ms |
| 3 | 1266.98 ms | 1270.39 ms |

## 20. Persistent Volume

The controlled light-index digest was `b83bb64ea9c8bb79a3f5060b0125d746a62c02ad0216c92177cff8dd60366b83` before and after three restarts, container recreation, three stop/start cycles, load, and final observation. The four Compose named volumes remained present after final `docker compose down`. `CONTAINER_PERSISTENCE_VALID = TRUE`.

## 21. Recreation

A safe `docker compose up -d --force-recreate` changed the container ID from `fd937bdf...496507` to `460aa87e...667683` without deleting volumes. The recreated service reached live in 1725.13 ms and ready in 1729.72 ms, returned degraded 200, passed ABSTAIN smoke, and retained the exact index digest.

## 22. Index Integrity

The light-mode controlled manifest contained one synthetically authored document with valid `page_content` and metadata schema. Readiness validated existence, JSON openability, non-empty chunk count, and document schema on every lifecycle. No silent rebuild or mutation occurred. The private 7333-chunk Chroma index was never copied into the image or destructively tested; its frozen logical digest remains reused read-only evidence.

## 23. Shutdown ×3

`docker compose stop -t 30 api` passed 3/3 with measured command latencies 848.84, 774.36, and 781.24 ms. Each stopped container exited 0, was not OOM-killed, emitted `Application shutdown complete`, restarted without cleanup, returned ready/degraded, and retained the index. `CONTAINER_SHUTDOWN_VALID = TRUE`.

## 24. Load Smoke

Real container HTTP, 24 requests per primary level:

| Concurrency | Success | Errors | Median | p95 | Max |
|---:|---:|---:|---:|---:|---:|
| 1 | 24/24 | 0 | 1.91 ms | 17.25 ms | 23.73 ms |
| 4 | 24/24 | 0 | 5.79 ms | 23.28 ms | 23.30 ms |
| 8 | 24/24 | 0 | 6.79 ms | 23.22 ms | 23.23 ms |

There were no unexplained 5xx, crashes, deadlocks, trace contamination, or index mutations. `CONTAINER_LOAD_SMOKE_VALID = TRUE`.

## 25. Latency

Concurrency 4/8 container p95 stayed near the native references (18.66/25.62 ms). A confirmatory 24-request c1 run remained 24/24 but had aggregate p95 9.11 ms versus the native 1.96 ms reference; route detail was live p95 1.98 ms and ready p95 9.11 ms. Per policy this is recorded as non-blocking `CONTAINER_PERFORMANCE_INVESTIGATION_REQUIRED`; no RAG tuning was attempted.

## 26. Resource Usage

Post-load `docker stats --no-stream` reported CPU 0.11%, memory 112 MiB / 15.54 GiB (0.70%), 61 PIDs, no OOM, and a healthy container. Image size was 162,451,362 bytes. These are observations, not tuned limits.

## 27. Provider

Provider requirement remains OPTIONAL. The initial local Compose substitution had one optional provider variable present, but no provider request was made and no value was printed. The certified keyless container was immediately recreated with both optional provider variables explicitly empty; readiness then correctly reported degraded. Final state: `REAL_PROVIDER_UNAVAILABLE / OPTIONAL_PROVIDER_CAPABILITY_NOT_VALIDATED`.

## 28. CI

`CI_CONFIGURED = TRUE`, `CI_EXECUTED = FALSE`, `CI_EXTERNAL_EXECUTION_PENDING = TRUE`, and `CI_HARD_BLOCKER = NO`. No remote, push, PR, or workflow action occurred.

## 29. Operational Fixes

One real Docker defect was fixed in `f0134f5`: production build context included test/evaluation-only files and a developer-specific path. Only `.dockerignore` changed, adding two exclusions. Dockerfile, Compose, entrypoint, Python runtime, frontend, and all RAG correctness semantics remained unchanged.

## 30. Final Baseline

The formal `V377_ALIGNED_BENCHMARK_V2` runner executed after container validation. Both internal runs had 69 formal rows, 54 correct, 9 FA, 3 FR, Accuracy 78.26%, AR 76.92%, AbR 79.07%, HN 10/10, and score-lineage fidelity 69/69. Rank sequences, decisions, verdicts, and digests matched.

## 31. Protected Hashes

`python -m backend.v382_release_guard` passed: 28/28 protected files, no drift/missing/unpinned entry, and `TRACKED_PRIVATE_FILES = 0`. RAG semantic diff is ZERO.

## 32. Tests

Actually rerun: Docker daemon/version probe, final Compose config, cold build and rebuild, two actual image-content audits, non-root execution, real Compose startup/readiness/public/request-ID/log/privacy/metrics smoke, restart 3/3, persistence and recreation, shutdown 3/3, 1/4/8 load plus one c1 confirmation, final Compose cleanup, formal V377 replay, and release guard. The 948-test backend suite and 600-second soak were not repeated because no Python runtime code changed.

## 33. Frontend Reuse

`REUSED_FROM_V383`: 34/34 frontend tests PASS, production build PASS, and CORS connectivity PASS. The frontend is separately hosted and no frontend file changed.

## 34. Private Safety

Actual image inspection and the final release guard both passed. `TRACKED_PRIVATE_FILES = 0`; no private PDF, benchmark payload/result, annotation, vector DB, `.env`, key/token, or private log entered Git or the final image. AutoCourse-RAG remained read-only.

## 35. Engineering Decision

All nine required container gates are real PASS: Compose config, image build, image content, runtime, health, restart, persistence, shutdown, and load. Correctness, protected hashes, private safety, and zero semantic diff also hold. The formal decision is `DEPLOYMENT_FAILURE_MODE_VALIDATION_READY`.

## 36. RC Manifest

`V383_RELEASE_CANDIDATE_MANIFEST.json` was generated. It records the validated source commit, Docker/image/container evidence, correctness freeze, operational fix, reused native/frontend evidence, known limitations, and pending external/optional gates.

## 37. Remaining External Gates

Core internal blockers: none. External/optional pending facts are remote CI execution and real provider ANSWER capability. `CONTAINER_PERFORMANCE_INVESTIGATION_REQUIRED` is a documented non-blocking operational follow-up.

## 38. NEXT_PHASE

`V3.84 Release Candidate Packaging & Project Evidence`. Do not execute it automatically and do not reopen Accuracy Research.
