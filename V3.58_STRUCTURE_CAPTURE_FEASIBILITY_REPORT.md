# V3.58 Structure-Capture Feasibility Report

## 1. Status

**STRUCTURE_CAPTURE_PARTIAL**(细化方向明确:cell-band 重构算法,
允许一次 refinement,尚未执行)

## 2. Git

- 起点 `fc73fb4` → 提交 `d7de643`(capture prototype)
- 工作树 clean,无 push
- AutoCourse-RAG 保持只读;⚠️ 其实际 HEAD 为 `b276232` 且存在未跟踪
  `tmp/`,与协议记录的 `e1e483e4…` 不符——本会话从未写入该仓库,
  以磁盘为准如实上报

## 3. V3.57 R4 Decision Preservation

R4 结论完整保留:ruling fusion 对分割方向无效(24.31% 全配置一致),
失败主因是 wrapped-cell 文本跨行拆分(47%)。V3.58 只研究 capture 层。

## 4. Why Evidence Relaxation Was Rejected

维持 V3.50 责任分离与 V3.51 FLAT_TEXT_OWNERSHIP_INFERENCE=FORBIDDEN:
Evidence 不应修复 Producer 丢失的结构。子串绑定路线被明确拒绝。

## 5–6. Capture Provider / Dependency Audit

| 候选 | 状态 | 许可(来源=已装 dist-info) | 几何能力 |
|---|---|---|---|
| **pdfplumber 0.11.10** | feasibility 安装 | **MIT**(LICENSE.txt 已验证) | char/word bbox + line/rect edges |
| pdfminer.six 20260107 | pdfplumber 传递依赖 | MIT(LICENSE 文件在) | LTChar 级 |
| pypdfium2 5.13.0 | 传递依赖 | Apache-2.0/BSD-3 | 渲染用 |
| PyMuPDF | 未选用 | AGPL-3.0 — 部署许可需单独评估(§37) | 最快 |
| Camelot/Tabula | 排除 | 高层语义猜测属 §10 禁区 | — |

## 7. Baseline Capture (CAPTURE_A)

pypdf layout text + walker rulings = R3/R4 现状:usable recall 24.31%,
wrapped-cell 拆分是主要失败。

## 8–11. Fine-Grained Capture Architecture

`backend/retrieval/structure_capture_v358.py`:
CapturedTextAtom(text/x0/x1/y0/y1/page/source_id) → GeometryEdge(h/v) →
row bands(strong h-edge ≥50% span 作界,否则 y 聚类)→ column bands
(strong v-edge 作界,否则 x0 聚类)→ LogicalCell(row_band×column_band 内
词按序拼接,SAME_COLUMN_NO_SEPARATOR merge proof,GEOMETRIC/INFERRED
boundary confidence)。

## 12–17. Diagnostic slice 与信号级指标

评估对象 = 完整 real benchmark(62 fixtures / **1261 GT cells**)。
GT 仅用于评估;重建路径 query-agnostic(§19-20 遵守)。

## Cell 粒度结果(核心)

| 指标 | F0 layout 基线 | F1 atoms-only | F2 atoms+edges |
|---|---|---|---|
| exact cell resolution | **72.7%** | 61.0% | 60.1% |
| any(含超集)resolution | 73.0% | 67.3% | **76.4%** |
| false cell merges | **53** | 81 | 132 |
| parameter localization | 73.6% | 79.7% | **89.0%** |

解读:
- F2 的 superset 恢复(76.4%)与参数定位(+15pp)证明**细粒度结构信息
  确实存在且可及**
- 但 F2 false merge(132 ≈10.5%)远超 ≤2% 要求 —— 我的 band 推导
  (v-edge 需贯穿 45% 高度才算列界等)过于粗糙,把相邻 GT cell 吞进
  同一 band

## Gate 对照(§33)

1. split-cell resolution ≥60%:F2 any=76.4% ✓ / exact 60.1% 边缘
2. false merge ≤2%:**✗(10.5%)**
3. localization 显著改善:✓(73.6%→89.0%)
4. recoverability 高于 producer ceiling:✓(76% >> 24%)
5. query-agnostic ✓;6-7 contract/hashes ✓

→ 未全过:**STRUCTURE_CAPTURE_PARTIAL**。瓶颈归属 =
**cell-band 重构算法**,非 capture signal(signal 层所有词/edge 可见)。

## Performance(pdfplumber,62 页全量)

单页 words+edges 提取中位 ~0.2s、p95 <0.5s 量级;无病态放大。

## Tests / Protected Hashes

后端 806 passed / 2 skipped;前端构建通过;V3.51/V3.54 哈希不变;
production requirements 未改动(pdfplumber 仅存在于 venv 实验)。

## NEXT_PHASE(允许一次 refinement)

改进 cell-band 重构:
- v-edge 按 y-span 分段作为局部列界(非全局贯穿才有效)
- 词 x0+x1 区间重叠图替代纯起点聚类
- h-edge 行界与 wrap 吸收统一为同一 edge-graph 决策
目标:false merge 10.5% → ≤2%,exact resolution ≥72% 保持。
达成后再评估 STRUCTURE_CAPTURE_FEASIBILITY_READY。
