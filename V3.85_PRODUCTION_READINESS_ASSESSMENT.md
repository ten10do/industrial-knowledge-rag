# V3.85 Production Readiness Assessment

**Status: `EXPERIMENT_VALID`（Phase B 实跑完成）。** 离线仿真与实跑结果一致；对照组复现冻结 digest `dcd54890…`，等价性 PASS；两次实跑确定性回放 PASS；938 项后端回归测试通过。生产默认保持关闭（env 门控），工作点决策见 §10。

生成命令：`python scripts/v385_production_readiness_analysis.py`（离线仿真）、`python scripts/v385_contract_distance_gate.py 1.20|1.35`（实跑实验）
输入制品：`results/v377_aligned_baseline/run1_full_trace.json`（run1，digest `dcd548906ef2b1f233282283b8684f8c82066dc5eea9f0179ff45a4034a5aa1d`）、`backend/evaluation/benchmark_private/v377_alignment/aligned_benchmark_v2.json`
实跑产物：`results/v385_contract_distance_gate/`（control / gated τ=1.20 / gated τ=1.35 三组 REAL_EVIDENCE_RUNTIME 轨迹）

---

## 0. 结论摘要

1. **78.26% 是被错误选中的指标。** 该数字把两类代价相差一个数量级的错误等权相加。真正决定安全性的量是「不可回答输入上的错误回答率」，去重后该值为 **6/13 = 46.15%**（`CORPUS_UNSUPPORTED` 切片），而非 21.74%。

2. **准确率几乎提不上去，但安全加权风险可以降 42%。** 分割半验证（300 次随机划分，τ 只在校准半上选，在留出半上评）：留出准确率 79.19% → 80.48%（**Δ +1.29pp，sd 5.66pp，不显著**）；安全加权风险 43.5 → 25.1（**Δ −42.4%**）。这是一条真实的、稳健的改进，但它不体现在准确率上。

3. **「人工复核」按常规设计基本无效。** 需要改成对抗式复核协议才能产生实质收益（证据见 §6.2）。

4. **基准集本身有缺陷，必须先修。** 69 条中有 6 条完全重复（8.7% 冗余），且其中 2 条重复项本身是错误样本，导致「9 个错误回答」实为 **8 个**。

5. **一个直觉上正确、实测错误的方案已被否决**：对无设备锚定的参数查询一律拒答，会消灭 7 个错误回答但同时摧毁 15 个正确答案，准确率 79.37% → 66.67%。

---

## 1. 当前基准的真实结构

### 1.1 发布口径与去重口径

| 口径 | n | correct | 错误回答 FA | 错误拒答 FR | PARTIAL | 准确率 |
|---|---|---|---|---|---|---|
| 发布（KNOWN_LIMITATIONS.md） | 69 | 54 | 9 | 3 | 3 | 78.26% |
| 去重后 | 63 | 50 | 8 | 3 | 2 | 79.37% |

重复组（`query_text` 逐字节相同）：

| 重复组 | ID |
|---|---|
| tell me about the sinamics g120 drive. | Q0021, Q0027 |
| tell me about the acs580 drive. | Q0022, Q0028 |
| tell me about the altivar atv320 drive. | Q0023, Q0029 |
| tell me about the powerflex 520 drive. | Q0024, Q0030 |
| tell me about the fr-e800 drive. | Q0025, Q0031 |
| tell me about the fc51 drive. | Q0026, Q0032 |

Q0021/Q0027 是同一个错误回答被计了两次；Q0022/Q0028 是同一个 PARTIAL 被计了两次。**任何「修复了 N 个错误」的声明在去重前都会被放大。**

### 1.2 切片分解（去重后）

| 切片 | n | correct | FA | FR | PARTIAL | 准确率 | **错误回答率** |
|---|---|---|---|---|---|---|---|
| ANSWERABLE | 25 | 20 | 0 | 3 | 2 | 80.00% | 0.00% |
| **CORPUS_UNSUPPORTED** | **13** | **7** | **6** | 0 | 0 | 53.85% | **46.15%** |
| HARD_NEGATIVE | 10 | 10 | 0 | 0 | 0 | 100.00% | 0.00% |
| OOD | 15 | 13 | 2 | 0 | 0 | 86.67% | 13.33% |

风险高度集中且极不均匀：`HARD_NEGATIVE` 已完全解决，`ANSWERABLE` 的错误拒答是可用性成本而非安全隐患，真正的危险全部压在 `CORPUS_UNSUPPORTED` 上。**把这四个切片混成一个准确率，等于把 46% 的错误回答率稀释成 21.74%。**

---

## 2. 15 个错误样本的归因

| 决策原因 | 计数 | 通道 |
|---|---|---|
| CONTRACT_REQUIREMENTS_COVERED | 7 | 契约层判定「关键需求已全部覆盖」→ 回答 |
| STRONG_VECTOR_EVIDENCE | 3 | 契约层**未抽取到**任何关键需求 → 落到距离分支 → 回答 |
| EXACT_MODEL_EVIDENCE | 2 | 型号精确匹配 → 回答但内容不完整 |
| MISSING_ATTRIBUTE_EVIDENCE | 1 | 契约层判定「属性证据缺失」→ 拒答 |
| MODEL_MISMATCH | 1 | 型号不匹配 → 拒答 |
| MISSING_ACTION_EVIDENCE | 1 | 契约层判定「操作证据缺失」→ 拒答 |

**15 个错误中有 12 个（80%）归因于契约层**：9 个是契约层主动做出的判定（7 覆盖 + 2 缺失），3 个是契约层静默（未抽取到需求，退化为纯向量距离判定）。

这与项目既有记录一致：`V3.79` claim-support validator 未达生产可用，`V3.80` 需求抽取升级未改善安全性，`V3.81` spec-existence lattice 在精度 0.61 / 门限 0.95 下 NO-GO。**契约层是一个未通过验证的研究组件，当前被接在生产决策主链路上。这才是生产可用性的真实瓶颈，而非准确率数值。**

### 2.1 全局距离阈值几乎没有判别力

整个基准只有 **1 个**全局阈值 `13.2347`。各判定类别的距离比（top1 距离 / 阈值）分布：

| 判定 | n | ratio 最小 | ratio 最大 |
|---|---|---|---|
| CORRECT | 20 | 0.52 | 1.81 |
| ABSTAIN_CORRECT | 30 | 0.81 | 2.82 |
| PARTIAL_INCOMPLETE | 2 | 0.92 | 0.93 |
| INCORRECT | 11 | 0.79 | 2.86 |

四类完全重叠。不存在任何阈值能把正确回答（0.52–1.81）与错误回答（0.79–2.86）分开。

### 2.2 ANSWERABLE 与 CORPUS_UNSUPPORTED 在查询侧不可分

| ID | 查询 | 切片 | 结果 |
|---|---|---|---|
| Q0011 | What is the operating voltage range? | ANSWERABLE | CORRECT |
| Q0013 | What is the ambient temperature? | ANSWERABLE | CORRECT |
| Q0014 | What is the protection rating? | ANSWERABLE | CORRECT |
| Q0015 | What is the control method? | ANSWERABLE | CORRECT |
| Q0018 | What is the power rating? | ANSWERABLE | CORRECT |
| Q0012 | What is the output frequency? | CORPUS_UNSUPPORTED | **FALSE_ANSWER** |
| Q0016 | What is the overload capacity? | CORPUS_UNSUPPORTED | **FALSE_ANSWER** |
| Q0017 | What is the response time? | CORPUS_UNSUPPORTED | **FALSE_ANSWER** |
| Q0019 | What is the efficiency class? | CORPUS_UNSUPPORTED | **FALSE_ANSWER** |
| Q0020 | What is the noise level? | CORPUS_UNSUPPORTED | **FALSE_ANSWER** |

两组查询在句法上完全同构，唯一差别是语料是否恰好覆盖该参数。**这个差别不存在于查询本身，也不存在于检索到的片段中，只存在于「语料整体覆盖情况」这个系统当前从未显式建模的量上。**

被否决的干预（脚本 `[6]`）：对匹配 `^what is the .+$` 的 26 条无锚定查询一律拒答 → 消灭 7 个错误回答，摧毁 15 个正确答案，**准确率 79.37% → 66.67%。REJECTED。**

---

## 3. 可行干预：契约分支检索距离上限

### 3.1 问题定位

`backend/retrieval/evidence.py:456`：

```python
elif contract.has_critical_requirements:
    decision = Decision.ANSWER          # 无任何检索距离检查
    ...
elif vector_distance is not None and vector_distance <= policy.max_vector_distance:   # :468
```

契约分支在回答时**完全绕过**了距离闸门；距离闸门只在契约层没抽到需求时才生效。这解释了全部高距离错误回答：

| ID | ratio | 切片 | 查询 |
|---|---|---|---|
| Q0033 | 2.86 | OOD | What is the capital of France? |
| Q0019 | 1.96 | CORPUS_UNSUPPORTED | What is the efficiency class? |
| Q0020 | 1.53 | CORPUS_UNSUPPORTED | What is the noise level? |
| Q0043 | 1.46 | OOD | What is the speed of light? |
| Q0016 | 1.41 | CORPUS_UNSUPPORTED | What is the overload capacity? |

### 3.2 权衡曲线（去重后 63 条）

| τ | correct | FA | FR | PARTIAL | 准确率 | 风险 |
|---|---|---|---|---|---|---|
| 基线 | 50 | 8 | 3 | 2 | 79.37% | 85 |
| 1.15 | 48 | 2 | 11 | 2 | 76.19% | 33 |
| **1.20** | **50** | **2** | 9 | 2 | **79.37%** | **31** |
| 1.30 | 51 | 3 | 7 | 2 | 80.95% | 39 |
| **1.35** | **53** | **3** | 5 | 2 | **84.13%** | 37 |
| 1.40 | 53 | 3 | 5 | 2 | 84.13% | 37 |
| 1.45 | 52 | 4 | 5 | 2 | 82.54% | 47 |
| 1.55 | 50 | 6 | 5 | 2 | 79.37% | 67 |

风险口径可审计：`risk = 10 × FA + 1 × FR + 1 × PARTIAL`。

存在两个有意义的工作点：

- **保守点 τ = 1.20**：准确率与基线**完全相同**（79.37%），错误回答 8 → 2（−75%），代价是错误拒答 3 → 9。**准确率零损失换来的纯安全收益。**
- **均衡点 τ = 1.35**：准确率 79.37% → 84.13%，错误回答 8 → 3，错误拒答 3 → 5。

### 3.3 过拟合检验（分割半，300 次）

τ 在 31 条校准半上选取，在 32 条留出半上评估，目标为 `(accuracy, -risk)`：

| 指标 | 基线 | 加闸门 | Δ |
|---|---|---|---|
| 留出准确率 | 79.19% (sd 5.48pp) | 80.48% (sd 5.66pp) | **+1.29pp（不显著）** |
| 留出风险 | 43.5 (sd 14.2) | 25.1 (sd 13.6) | **−42.4%** |
| 选中 τ 分布 | | | 1.35×163, 1.85×59, 1.20×33, 1.30×29 |

**诚实结论：准确率的提升没有被验证（+1.29pp 远小于 sd 5.66pp），风险的下降是稳健的。** 任何以「准确率提升到 84%」为由的论证都不成立；成立的论证是「在准确率不变的前提下把安全加权风险降低 42%」。

τ 的选择在 300 次中有 163 次落在 1.35，说明存在稳定的优势区间（1.20–1.40 全部优于或持平基线），但 1.85 出现 59 次说明边界在 n=63 下依然脆弱。**1.53（可修）与 1.70（会破坏）之间只有 2 个样本，这是脆弱性的根源。**

### 3.4 τ = 1.35 的残余错误（10 项）

| ID | 切片 | 类型 | ratio | 查询 |
|---|---|---|---|---|
| Q0012 | CORPUS_UNSUPPORTED | FALSE_ANSWER | 0.99 | What is the output frequency? |
| Q0017 | CORPUS_UNSUPPORTED | FALSE_ANSWER | 1.22 | What is the response time? |
| Q0021 | CORPUS_UNSUPPORTED | FALSE_ANSWER | 0.79 | Tell me about the SINAMICS G120 drive. |
| Q0006 | ANSWERABLE | FALSE_REFUSAL | 1.43 | What is the rated current setting? |
| Q0014 | ANSWERABLE | FALSE_REFUSAL | 1.81 | What is the protection rating? |
| Q0018 | ANSWERABLE | FALSE_REFUSAL | 1.70 | What is the power rating? |
| Q0049 | ANSWERABLE | FALSE_REFUSAL | 0.93 | How does pulse-width modulation work? |
| Q0056 | ANSWERABLE | FALSE_REFUSAL | 1.27 | How do you select wire gauge for motor connection? |
| Q0022 | ANSWERABLE | PARTIAL | 0.93 | Tell me about the ACS580 drive. |
| Q0055 | ANSWERABLE | PARTIAL | 0.92 | Describe the function of an emergency stop circuit. |

---

## 4. 残余风险分属三种性质，需要三种不同手段

### 4.1 高相似但无支持（Q0012 ratio 0.99、Q0021 ratio 0.79）

距离闸门在原理上抓不到这两条。这不是本项目特有的偶发问题：嵌入相似度与「是否真正被证据支持」在分布尾部是退化的。

> Sinha, D. *The Semantic Illusion: Certified Limits of Embedding-Based Hallucination Detection in RAG Systems*. arXiv:2512.15068. 对真实幻觉（HaluEval），嵌入方法在 95% 目标覆盖率下 FPR 达到 **100%**；NLI 的 AUC 为 0.81 但仍不足以支撑安全保证；GPT-4 作为 judge 为 7% FPR（95% CI [3.4%, 13.7%]）。
> 来源：https://arxiv.org/pdf/2512.15068

需要的手段是**构建期覆盖度索引**：在入库时为 `(equipment_model, parameter_name)` 二元组建立确定性记录，标明语料是否给出该参数的值、给出几个互相冲突的值。决策时对无锚定参数查询查表：单设备覆盖 → 带作用域回答；多设备冲突 → 请求澄清；零覆盖 → 拒答。这把一个「模型必须从查询猜出来的问题」变成「查一张构建期生成的表」，符合确定性计算优先于模型判断的原则。

**Q0021 是另一个可定位的缺陷。** `SINAMICS G120` 在 `backend/retrieval/document_identity_v373.py:30` 的身份词表中存在，身份是可解析的，但身份闸门未触发，查询落到 `STRONG_VECTOR_EVIDENCE` 分支（ratio 0.79）被回答。同为语料外设备的 Q0023–Q0026 都正确拒答了。**这是身份闸门上的一个具体缺口，需要插桩确认触发条件为何未满足**（`foreign_equipment_signal` / `known_identity` 的具体取值）。本轮未做该插桩，不臆断根因。

### 4.2 OOD（Q0033、Q0043）

已由 τ 闸门覆盖。

### 4.3 解释性 / 程序性问题（Q0006、Q0049、Q0056）

系统对近乎同构的问法给出相反行为：

| 正确回答 | 错误拒答 |
|---|---|
| Explain Profinet communication basics. (Q0052) | How does pulse-width modulation work? (Q0049) |
| What is a variable frequency drive? (Q0048) | How do you select wire gauge for motor connection? (Q0056) |
| What is the purpose of an overload relay? (Q0051) | What is the rated current setting? (Q0006) |

契约层对「How does X work / How do you select」抽取了 VALUE/ATTRIBUTE/ACTION 类需求，而程序性解释天然不提供这些证据结构，导致拒答。需要**查询类型路由**：事实查表型走契约路径，解释说明型走独立的充分性判据。这是 3 个错误拒答的唯一成因，也是全部残余错误中唯一一类「纯粹损失、无安全收益」的错误。

---

## 5. 残余错误无法通过调阈值消除的原因

Q0012（0.99）与 Q0021（0.79）低于阈值，Q0014（1.81）与 Q0018（1.70）高于 1.53。任何单一距离阈值都无法同时保留后两者并拒答前两者。**继续在标量阈值上做一维搜索的边际收益已经耗尽**，下一步的增益只能来自引入新的判别信号（覆盖度索引、查询类型路由），而非继续调参。

---

## 6. 对两个拟议方案的评估

### 6.1 「继续保持非生产认证定位」

必要，但不充分，且当前形态偏弱。定位声明是**标签**，标签不降低风险。一个被标注为「非生产认证」但在现场工程师触达范围内的系统会被当作生产系统使用。要让定位产生实际约束力，需要机制化：

- 运行时在每条回答上强制附加不可关闭的定位标记与置信等级；
- 导出物（PDF / 报告 / 截图）继承该标记，避免脱离上下文后裸奔；
- 对安全关键参数类（额定值、保护定值、安全功能）默认走拒答并要求引用原文页码，把「非认证」落到具体行为上而非页脚声明上。

### 6.2 「增加人工复核」

按常规设计基本无效，按对抗式设计才有效。

支持证据：

> *Bias in the Loop: How Humans Evaluate AI-Generated Suggestions*. arXiv:2509.08514. n = 2,784 随机实验。**要求评审者修正被标记出的 AI 错误，反而降低参与度并提高对错误建议的接受率**；对 AI 的态度是表现最强的预测因子，自动化倾向高的参与者表现出危险的过度依赖；**经济激励对表现没有一致影响**。
> 来源：https://arxiv.org/html/2509.08514v1

> *(Human) Attention Is (Still) All You Need*. arXiv:2606.12848. 2×4 析因实验，280 次完整流程。无约束多智能体基线的关键失败率 **72%**；在相同模型、相同提示下，通过「LLM 只推理不执行数据工作、数据与估计确定性执行、三个绑定的人工决策门」把失败率降到 **16%**（Fisher 精确检验 p < 0.001）。80 次消融表明**确定性计算与人工门各自独立贡献**。
> 来源：https://arxiv.org/abs/2606.12848

> *Studying Quality Improvements Recommended via Manual and Automated Code Review*. arXiv:2602.11925. 在同一批 PR 上，ChatGPT-4 只能发现人类评审者报告质量问题的 **10%**。
> 来源：https://arxiv.org/abs/2602.11925

由此得出复核协议的三条硬性设计约束：

1. **不展示结论，只展示证据。** 评审者看到的是被确定性抽取出来的原文片段（文档 + 页码 + 原句），被问的是「这段话是否陈述了该主张」，而不是看到一段流畅答案后问「这对不对」。后者正是自动化偏差的作用面。
2. **默认怀疑，而非默认通过。** 界面默认状态应为「未确认」，通过需要评审者显式定位到支持句；不提供「全部接受」批量操作（对应 2509.08514 中「要求修正反而降低参与度」的发现）。
3. **把确定性部分从模型中拿走。** 参数提取、数值比对、跨设备一致性检查必须是确定性代码，模型只承担语言组织。对应 2606.12848 中贡献最大的一项。

### 6.3 「受控场景白名单」

方向正确，但按**单条问题**定义白名单不可行：不泛化，且维护成本无上界。应按**查询类**定义：

```
白名单 = (equipment_model ∈ 语料已确认设备)
       × (parameter ∈ 构建期覆盖度索引中「单设备无冲突」集合)
       × (question_type ∈ {事实查表})
```

系统已有 `query_domain`、身份关系、support state 三个可用字段，构建期覆盖度索引补齐后即可表达。白名单外一律拒答并给出拒答理由分类。

---

## 7. 建议路线

| 阶段 | 内容 | 出口条件 | 状态 |
|---|---|---|---|
| **A. 度量可信化**（不动运行时） | 去重基准至 63 条；把准确率拆成 FA / FR / PARTIAL 三指标并分切片发布；引入可审计的风险指标 | 报告数字与去重口径一致，指标不可再混用 | 已交付（本报告） |
| **B. 保守工作点**（改 1 处代码） | 在 `evidence.py:456` 契约分支加检索距离上限，取 τ = 1.20 | 实跑产出 `REAL_EVIDENCE_RUNTIME` 轨迹并与基线 digest 对比，等价性 PASS | **已完成，见 §10**；生产默认保持关闭，由部署方显式开启 |
| **C. 结构补齐**（新增能力） | 构建期覆盖度索引；设备维度覆盖度闸门；Q0021 身份闸门缺口插桩；白名单机制规格 | 覆盖度闸门在独立留出集上验证 PASS（G1–G5），Q0021/Q0027 修复且零 BROKEN | **已完成，见 §11** |

**B 阶段选 τ = 1.20 而非 1.35 的理由**：1.35 的准确率增益未被分割半验证支持（+1.29pp vs sd 5.66pp），而 1.20 在准确率完全不变的前提下把错误回答从 8 降到 2。在安全关键语境下，用零准确率损失换取 75% 的错误回答削减，比追求一个统计上不成立的准确率数字更稳健。

**C 阶段之后**再引入 conformal 校准，把「τ 是多少」从手工调参替换为带覆盖保证的程序化选择：

> Chakraborty et al. *Response Quality Assessment for RAG via Conditional Conformal Factuality*. arXiv:2506.20978. Conformal-RAG 在目标事实性 85% 下的子声明移除率仅 **8.9%**（Conformal-LLM 为 86.8%），且实证事实性始终不低于目标。
> Xu, Yunpeng et al. *Selective Conformal Risk Control*. arXiv:2512.12844. 两阶段：先对低置信样本弃权，再对被接受子集做 conformal 风险控制。
> 来源：https://arxiv.org/pdf/2506.20978v1.pdf , https://arxiv.org/html/2512.12844v2

注意 2512.15068 的负面结果：**conformal 只能把已有的判别信号标定成覆盖率，不能创造判别信号。** 在覆盖度索引建立之前上 conformal，只会得到一个「保证 95% 覆盖但 FPR 100%」的阈值，形式上严谨、实质上无用。顺序不可颠倒。

---

## 8. 未验证事项与阻塞项

| 项 | 状态 | 说明 |
|---|---|---|
| τ 闸门的实际效果 | `LIVE_VALIDATED` | 实跑（REAL_EVIDENCE_RUNTIME，fidelity 69/69，确定性回放 PASS）与离线仿真逐条一致，见 §10 |
| 覆盖度索引可行性 | `VALIDATED` | 从 `vector_db_v369` 实索引抽取 7333 条正文，与冻结 catalog / manifest 三重交叉验证全 PASS，见 §11.1 |
| Q0021 身份闸门缺口根因 | `VALIDATED` | 插桩实证：查询侧身份提取为语料驱动（`identities_from_query` 对语料外设备返回空身份）+ `foreign_equipment_signal` 为厂商粒度缺陷，见 §11.3 |
| τ 的泛化性 | 脆弱 | n = 63，可修（1.53）与会破坏（1.70）之间仅 2 个样本。需要独立留出集，不能使用本基准；Phase C 提供的是与 τ 正交的设备维度信号，不改变 τ 本身的样本量约束 |
| 运行时改动 | 已执行（默认关闭） | 仅 `backend/retrieval/evidence.py` 增加默认关闭的 `contract_max_distance_ratio` 字段；对照组 digest 与冻结 digest 一致，证明默认路径零行为变化。未触碰 `vector_db_v369` |
| 生产默认工作点 | 待决策 | 部署方显式设置 `EVIDENCE_CONTRACT_MAX_DISTANCE_RATIO` 后生效；默认不开启 |

---

## 9. 本次产生的文件

| 文件 | 说明 |
|---|---|
| `V3.85_PRODUCTION_READINESS_ASSESSMENT.md` | 本报告 |
| `scripts/v385_production_readiness_analysis.py` | 可复现分析脚本，输出本文全部数字 |
| `scripts/v385_contract_distance_gate.py` | 实跑实验脚本（control + gated，双跑确定性回放） |
| `scripts/v385_runtime_smoke.py` | 运行时复现冒烟（8 个跨度样本，delta 0.00e+00） |
| `results/v385_contract_distance_gate/` | 实跑产物：control / τ=1.20 / τ=1.35 三组轨迹与实验摘要 |
| `backend/retrieval/evidence.py` | 新增默认关闭的 `contract_max_distance_ratio` 策略字段 |
| `V382_RESEARCH_FREEZE_MANIFEST.json` | 冻结清单推进到 `V385_RESEARCH_FREEZE_MANIFEST_V1`，基线不变 |

**Phase C 新增文件（§11）**

| 文件 | 说明 |
|---|---|
| `backend/retrieval/coverage_gate.py` | 设备维度语料外检测前闸门（纯新增模块，禁用即零行为变化） |
| `scripts/v385_coverage_index_build.py` | 从 `vector_db_v369` 实索引抽取全正文，产出 `chunk_catalog_v385_fulltext.json` |
| `scripts/v385_coverage_gate_build.py` | 参数覆盖度索引构建，产出 `coverage_index_v385.json`（1799 参数 / 3 模型） |
| `scripts/v385_coverage_signal_probe.py` | 属性词覆盖度判别力探针（否决该信号方向的实证） |
| `scripts/v385_q0021_identity_probe.py` | Q0021 身份闸门缺口插桩（根因实证） |
| `scripts/v385_coverage_gate_exp.py` | 覆盖度闸门全量实验运行器（control×2 + gated×2） |
| `scripts/v385_coverage_gate_holdout.py` | T5 独立留出集验证（G1–G5 协议） |
| `test_coverage_gate.py` | coverage_gate 正式单元测试（14 用例全 PASS） |
| `backend/evaluation/benchmark_private/v377_alignment/corpus/chunk_catalog_v385_fulltext.json` | 全正文分块目录（16.4MB，运行时产物，gitignore） |
| `backend/evaluation/benchmark_private/v377_alignment/corpus/coverage_index_v385.json` | 覆盖度索引（运行时产物，gitignore） |
| `results/v385_coverage_gate/experiment.json` | 全量实验产物（运行时产物，gitignore） |
| `results/v385_coverage_gate/holdout.json` | T5 留出集验证产物（运行时产物，gitignore） |

---

## 10. Phase B 实跑验证结果（EXPERIMENT_VALID）

运行器：`scripts/v385_contract_distance_gate.py`，复用 `v377_aligned_baseline.py` 的全部函数，真实 Chroma 检索 + 真实 V3.76 score lineage。每组条件跑两遍并要求确定性回放一致。

### 10.1 等价性检查（闸门关闭时零行为变化）

| 项 | 值 |
|---|---|
| 冻结基线 digest | `dcd548906ef2b1f233282283b8684f8c82066dc5eea9f0179ff45a4034a5aa1d` |
| 对照组 digest（新代码，默认关闭） | `dcd548906ef2b1f233282283b8684f8c82066dc5eea9f0179ff45a4034a5aa1d` |
| **EQUIVALENCE_OK** | **True** |
| 对照组与门控组确定性回放 | 均 PASS |
| score lineage fidelity | 69/69（两组各条件） |
| eligible_formal / record_origin | 69/69，全部 `REAL_EVIDENCE_RUNTIME` |

这证明新增的 `contract_max_distance_ratio` 字段在默认关闭时对决策零影响，冻结基线的含义完整保留。

### 10.2 实跑 before / after（发布口径 69 条）

| 指标 | 基线 | τ=1.20 | τ=1.35 |
|---|---|---|---|
| correct | 54 | 54 | **57** |
| 准确率 | 0.7826 | 0.7826 | **0.8261** |
| 错误回答 FA | 9 | **3** | **4** |
| 错误拒答 FR | 3 | 9 | 5 |
| PARTIAL | 3 | 3 | 3 |
| 风险（10×FA + FR + PARTIAL） | 96 | **42** | **48** |

### 10.3 实跑 before / after（去重口径 63 条）

| 指标 | 基线 | τ=1.20 | τ=1.35 |
|---|---|---|---|
| correct | 50 | 50 | 53 |
| 准确率 | 0.7937 | 0.7937 | **0.8413** |
| 错误回答 FA | 8 | **2** | 3 |
| 错误拒答 FR | 3 | 9 | 5 |
| 风险 | 85 | **31** | 37 |

### 10.4 判定翻转明细

τ=1.20：12 处翻转。修复 6（Q0016/Q0017/Q0019/Q0020 属 CORPUS_UNSUPPORTED，Q0033/Q0043 属 OOD），破坏 6（Q0008/Q0014/Q0018/Q0048/Q0050/Q0051 全部属 ANSWERABLE）。与离线仿真预测逐条一致。

τ=1.35：7 处翻转。修复 5（Q0016/Q0019/Q0020/Q0033/Q0043），破坏 2（Q0014/Q0018）。

### 10.5 关键切片变化（发布口径）

| 切片 | 基线 acc | τ=1.20 | τ=1.35 |
|---|---|---|---|
| ANSWERABLE | 0.7692 | 0.5385 | 0.7308 |
| CORPUS_UNSUPPORTED | 0.6111 | **0.8333** | 0.7778 |
| HARD_NEGATIVE | 1.0000 | 1.0000 | 1.0000 |
| OOD | 0.8667 | **1.0000** | 1.0000 |

### 10.6 生产接线建议

- **默认保持关闭**，部署方显式设置 `EVIDENCE_CONTRACT_MAX_DISTANCE_RATIO` 后生效。原因：冻结基线 digest 的语义与默认策略绑定，翻默认值属于发布级 Gate 决策。
- 安全关键场景推荐 **τ = 1.20**：准确率零损失，FA 9→3，风险 96→42。代价是 ANSWERABLE 切片多 6 个错误拒答。
- 需要更高可用性时可选 **τ = 1.35**：准确率 0.7826→0.8261，FA 9→4。该增益在分割半验证中不显著（§3.3），实跑确认存在但与噪声同量级。
- τ 的选择不可在本基准上继续微调（§5），后续增益依赖 Phase C 的新判别信号。

### 10.7 清单与守卫

`V382_RESEARCH_FREEZE_MANIFEST.json` 已推进到 `V385_RESEARCH_FREEZE_MANIFEST_V1`：更新 `evidence.py` 哈希为 `79b51098…`，基线（54/69，digest `dcd54890…`）保持不变，新增 `v385_contract_distance_gate` 段记录本实验。守卫 `backend/v382_release_guard.py` PASS，回归测试 949 passed / 2 skipped。V383/V384 清单为历史发布记录，未改写。

---

## 11. Phase C 结构补齐执行结果（COMPLETED）

运行原则与 Phase B 一致：等价性优先（新模块禁用时冻结 digest 逐位不变），所有结论基于真实运行证据，禁止回填。

### 11.1 T1–T2 覆盖度索引（可行性实证）

`vector_db_v369/chroma.sqlite3` 实索引结构确认：单 collection 7333 条 embeddings；关联链 `embeddings.id = FTS rowid` 全命中（7333/7333）；chunk 正文在 `embedding_fulltext_search.string_value`；元数据在 `embedding_metadata`。冻结的 `chunk_catalog.json` 无正文（仅 chroma_id / source / page / text_len），正文必须从实索引抽取。

| 产物 | 验证 | 结果 |
|---|---|---|
| `chunk_catalog_v385_fulltext.json` | 数量 == 7333、chroma_id 集合 == 冻结 catalog、source 分布 == manifest（4821/1877/635）、0 空正文 | 全 PASS，sha256 `60e15b6a7b40…` |
| `coverage_index_v385.json` | 3 模型（acs580 1497 / s7-1200 329 / modicon m221 77），1799 个参数标识符 | 构建完成 |

**属性词级覆盖度无判别力（实证否决）**：探针（`v385_coverage_signal_probe.py`）对 54 条 What-is 类查询扫描「全词同现 chunk 数」，ANSWERABLE 与 CORPUS_UNSUPPORTED 完全重叠（Q0001 output=1334 vs 正确答案同量级）。「词出现」不等于「有定义支撑」，该信号方向关闭，Phase C 收敛为设备维度闸门。

### 11.2 白名单机制规格（T4）

受控场景白名单定义为三元组，与本报告 §6.2 一致：

```
白名单 = (equipment_model ∈ 语料已确认设备)
       × (parameter ∈ 覆盖度索引「单设备无冲突」集合)
       × (question_type ∈ {事实查表})
```

| 维度 | 实现状态 | 依据 |
|---|---|---|
| 设备维度 | **已实现**（`coverage_gate.py` + 全量实验 + 留出集） | 构建期全局 known_models（s7-1200 / acs580 / modicon m221），不依赖检索窗口 |
| 参数维度 | 规格就绪（未接线） | `coverage_index_v385.json`：1799 个参数中**单设备无冲突 1713 个（95.2%）**（acs580 1426 / s7-1200 246 / m221 41），可直接作参数白名单；86 个跨设备冲突参数需附加设备限定 |
| 问法维度 | 规格就绪（未接线） | 现有 `evidence_querytype.py` 是 V3.27 槽位抽取路由（VERIFICATION / EXTRACTION / UNKNOWN），与白名单维度不同；白名单问法集合收敛为 `{DIRECT_FACT, VALUE}`，与 `evidence.contract` 的 query_type 槽位对齐 |

设备维度优先落地的原因：Q0021 是唯一残余 FA 中可由**确定性信号**修复的（查询提及语料外设备），参数与问法维度需接入运行时策略层，留给部署方按风险偏好显式开启。

### 11.3 T3 Q0021 身份闸门缺口插桩（根因实证）

插桩复刻 `evidence.py:318-403` 中间态（`v385_q0021_identity_probe.py`），不动 `evidence.py`：

```
analyze_query("Tell me about the SINAMICS G120 drive.")
  -> manufacturer / equipment_model / product_series / product_family / identity_confidence 全空
has_query_identity = False
known_identity / compatible_identity -> 短路为 True
foreign_equipment_signal -> vendor 粒度：siemens 因 S7-1200 手册在语料内被跳过
route -> STRONG_VECTOR_EVIDENCE（ratio 0.7862）-> 被回答（错误）
```

根因确认与 §4.1 假设一致：词表存在但门未触发，缺陷在查询侧身份提取的语料驱动约束与 `foreign_equipment_signal` 的厂商粒度，不在文档侧词表。

### 11.4 覆盖度闸门设计与修正（T4 实现）

`backend/retrieval/coverage_gate.py`：检测查询中具体型号提及（复用 `document_identity_v373.SERIES_PATTERNS` + `VENDOR_EQUIPMENT_TERMS` 具体词，品牌词剔除），提及不在构建期 known_models 则返回 `OUT_OF_CORPUS_MODEL`。纯新增模块，禁用时零行为变化。三次修正（均有实测证据）：

| 修正 | 问题 | 修复 |
|---|---|---|
| BUG1 | `sinamics` / `powerflex` 品牌词被当具体型号，Q0021 报 `sinamics,sinamics g120` | `_BRAND_TOKENS` 增补品牌级词，只保留具体型号 |
| BUG1b | `s7-1200` 连字符形态失配（normalize 折叠为空格）、acs580 误判语料外 | `_series_mentions` 双变体；`known_models=` 全局索引参数（不依赖检索窗口） |
| BUG1c | Q0067 将语料内 s7-1200 误判语料外（mention 模板带连字符 vs known key 空格） | `corpus_known_models` 双 key 收录（归一形 + casefold 原形） |

**全量实验**（`v385_coverage_gate_exp.py`，复用 v377 加载链与冻结 digest，control×2 + gated×2）：

| 项 | 值 |
|---|---|
| control digest == 冻结 digest `dcd54890…` | **PASS**（等价性） |
| gated 双跑 digest `b0ab7b71…` | **PASS**（确定性） |
| FIXED | Q0021、Q0027（CORPUS_UNSUPPORTED，错误回答 → 正确拒答） |
| BROKEN | 0 |
| NEUTRAL | 10 条（原本正确拒答的 reason 归类优化，verdict 不变） |
| 与 τ=1.20 正交性 | 去重后残余 FA 仅剩 Q0012（OCR 乱码根因，需参数表结构解析，成本高收益单一，归已知残余） |

### 11.5 T5 独立留出集验证（G1–G5 全 PASS）

留出集与 V377 基准无交集，未参与索引构建：

- `hms_m40_queries.json`：15 条真实标注查询（Anybus CompactCom M40，语料外域，含 answerable / evidence 标注）
- `v327_extract_benchmark/train.json`：39 条 EXTRACTION 查询（Corpus F：Micro800 / EL6652 / R88D-1SN-ECT，均不在 V377 语料内）

| 闸门 | 内容 | 结果 |
|---|---|---|
| G1 | 54 条真实查询零误触发（无设备锚点 / 语料外设备都不产生伪拒答） | **54/54 PASS** |
| G2 | 9 条独立措辞的语料外锚点（s7-1500 / acs880 / m241 / powerflex 520 / fr-e800 / fx3u / cj2m / vlt fc101 / sinamics g120 改述）全部拒答 | **9/9 PASS** |
| G3 | 5 条语料内锚点（s7-1200 / acs580 / modicon m221 多措辞）全部放行 | **5/5 PASS** |
| G4 | 窗口无关性（`documents=[]` vs 合成窗口逐位一致）+ 双 key 形态解析 | **PASS** |
| G5 | 覆盖度边界（信息性）：54/54 条真实查询的设备均不在厂商模式表覆盖内（Anybus / Micro800 / EL6652 / R88D 无对应模式） | 记录为模式覆盖盲区 |

G5 的含义需要如实陈述：这两个真实留出集只验证了闸门的**误触发安全性**（G1），其正确拒答路径（G2）是通过独立措辞的模式覆盖锚点验证的。对模式表未覆盖的厂商设备（如 HMS Anybus），闸门保持沉默，防护依赖 T4 白名单的问法/参数维度或语料扩展，不可宣称闸门已覆盖全部语料外设备。

### 11.6 单元测试与回归

`test_coverage_gate.py`：14 用例全 PASS（检测契约、双 key 形态、窗口回退、语料内/外判定、品牌词排除、模式盲区）。覆盖度闸门禁用态零行为变化已由 §11.4 等价性 PASS 证明，不影响冻结 digest。

### 11.7 Phase C 结论

1. Q0021 类（查询提及语料外设备）已由设备维度闸门修复：Q0021 / Q0027 FIXED，零 BROKEN，10 条 NEUTRAL 为 reason 归类优化。
2. 白名单机制三维度规格确定：设备维度已实现并验证，参数维度（1713 个单设备无冲突参数）与问法维度（`{事实查表}`）规格就绪，接线留给部署方。
3. 残余 FA 仅剩 Q0012（OCR 乱码片段 + 契约对概念提及过度乐观），独立根因，需参数表结构解析，成本高收益单一，维持已知残余定位。
4. 与 §7 路线一致：conformal 校准仍需在覆盖度索引之后引入，本阶段未启动。
