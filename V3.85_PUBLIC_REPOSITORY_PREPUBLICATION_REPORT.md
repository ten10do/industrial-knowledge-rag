# V3.85 Public Repository Pre-Publication Report

## 1. Status

**PUBLIC_REPOSITORY_PRECHECK_READY**

The owner selected the MIT License. The standard root LICENSE, README notice and third-party boundary are present. All required internal pre-publication gates pass. External writes remain unauthorized.

## 2. Git

The initial V3.85 precheck was committed at c2c12751d71d21b9aab17a2399fc52e636daf338 on main with a clean tree. The license closure adds one publication-only commit. Recent commit subjects are scoped and preserve the real engineering history; no squash, rebase or history rewrite is justified.

AutoCourse-RAG remained PRE_EXISTING_EXTERNAL_STATE, read-only, on main at a430235d28de0fe79c0790acc2c99220fc239d1b.

## 3. RC Integrity

V383_RELEASE_CANDIDATE_MANIFEST.json and V384_RC_FREEZE_MANIFEST.json parse and remain immutable. V3.83 deployment input hashes, the V3.83 manifest hash and all 28 protected semantic hashes match current frozen inputs.

V384 public-document hashes validate against the frozen V3.84 commit b4b75a1. README's current hash changes only because V3.85 adds the required MIT section; the historical V384 manifest is not rewritten. This report is the V3.85 publication-metadata overlay. RC runtime integrity errors: 0.

## 4. Current Remote State

No Git remote is configured. No remote was added.

## 5. Current Tag State

git tag --list is empty. No Git tag or GitHub Release exists.

## 6. Public Git Hygiene

The closure tree contains 399 tracked files. Sensitive-path/extension findings are zero: no tracked .env, PDF, office archive, private key, model weight, database, private benchmark directory, vector database, results tree or runtime data.

The largest tracked file is the 85,196-byte frontend lockfile; there is no unexpectedly large binary or corpus object.

## 7. History Secret Scan

The initial V3.85 audit of 118 commits, 395 unique historical paths and 589 reachable blobs is reused unchanged. Total blob bytes scanned were 6,352,831; the maximum blob was 85,196 bytes, so no object was skipped by the 5 MiB safety bound. The initial precheck commit and this closure's LICENSE/docs were separately covered by current-tree scans.

High-confidence OpenAI-style, GitHub, AWS, bearer-token and private-key detections: zero. Historical sensitive filename/path findings: zero. Exact credential-assignment review produced only empty values, placeholders, environment lookups, self-references, test fixtures and domain-rule false positives.

Five historical blobs across three files contain old Windows repository paths. They contain no username, credential, private corpus content or private file object and are sanitized at current HEAD. Classification: LOW_RISK_HISTORICAL_PATH_METADATA, not REAL_SECRET and not HISTORY_PRIVATE_FILE. No history rewrite is required by the security gate; the owner may still choose one separately.

## 8. Secret Scan

Current tracked high-confidence secret scan passed. Exact developer username scan passed.

The local ignored .env contains one configured provider credential. Its value was not printed or committed. The file is verified ignored and has never appeared in Git history. Classification: REAL_SECRET_LOCAL_IGNORED, not a publication leak. Empty/placeholder/test values elsewhere are TEST_FIXTURE or FALSE_POSITIVE.

## 9. Private Corpus Audit

TRACKED_PRIVATE_FILES=0 and HISTORY_PRIVATE_FILES=0. No PDF or private benchmark/index/result object exists in current history.

Vendor names appear only in authored fixtures, schemas, tests, metadata descriptions and research reports. No raw vendor manual, large manual excerpt or private aligned benchmark payload/query file is tracked. V3.77 explicitly records that PDFs, query texts, expectations and detailed ledgers remain ignored.

## 10. License

PROJECT_LICENSE=MIT. LICENSE_OWNER_DECISION=RESOLVED.

The root LICENSE contains the standard SPDX-compatible MIT text with Copyright (c) 2026 ten10do and no additional restriction.

Installed direct dependencies report permissive MIT/BSD/Apache/ISC-family licenses. pdfplumber reports MIT and remains research-only. PyMuPDF reports AGPL-3.0/commercial dual licensing, is not declared in product requirements and is not bundled in the Docker image. The experimental local NLI model card reports Apache-2.0; no weights/cache are tracked.

The project MIT License covers repository code/documentation only to the extent owned by the copyright holder. Third-party dependencies and external data/model weights retain their respective licenses. This inventory is not legal advice or a complete transitive-license opinion.

## 11. README Audit

PASS. The first screen states the industrial safety problem, frozen benchmark context, deployment evidence, Quick Start and Known Limitations. Architecture, demo, evaluation, provider/CI boundary and five-minute path are present. The README now links the root MIT LICENSE and preserves the third-party/external-data license boundary. It does not claim enterprise production readiness, universal accuracy, SLA or security certification.

## 12. Documentation Links

PASS. Repository-relative file links and README anchors were resolved with case-sensitive target comparison. File-link failures: 0. Anchor failures: 0. Manifest-referenced files/hashes also validate.

## 13. Absolute Path Scan

Current tracked content: PASS for Windows drive/user-home patterns, POSIX user-home paths and the exact developer username.

The five historical low-risk repository-path blobs are disclosed in section 7; current public files are clean.

## 14. Demo Audit

DEMO_REPRODUCIBLE=TRUE. scripts/v384_public_demo.py passed liveness, required readiness, supported answerability, wrong-model abstention, unknown-fault API refusal, metrics, request-ID, index immutability and experimental-default checks.

The legacy public operational smoke also passed. The V3.84 demo explicitly clears provider credentials; both paths require no private corpus or provider call.

## 15. CI Workflow Audit

PASS. One workflow, .github/workflows/ci.yml, provides:

- backend syntax, release/private guard, 948-test command, public smoke and offline evaluation;
- full dependency/import smoke;
- frontend npm ci, test and production build;
- Compose validation and Docker image build.

No job references a private corpus, external provider secret, developer path or local-only cache.

## 16. CI Permissions

PASS. Global permissions are contents: read. No secrets, GITHUB_TOKEN, artifact upload or write permission is referenced.

Only official actions are used at current major refs: checkout@v4, setup-python@v5 and setup-node@v4. They are not immutable commit-SHA pins; optional SHA pinning remains a supply-chain hardening note, not a false PASS claim.

## 17. Docker Public Build

PASS for the frozen RC. Dockerfile and Compose hashes match V3.83. The build installs only public requirements, copies the backend, runs non-root and uses no secret, private corpus/index or developer cache.

V3.83-C actual image/content validation and the unchanged V3.84 cache build/start are reused. docker compose config --quiet passed now. A full container gate was not repeated.

## 18. Gitignore

PASS after publication-hygiene hardening. Existing rules cover .env, virtual environments, Python/frontend caches, runtime data, results, private benchmark, indexes and screenshots. V3.85 adds explicit PDF, common model-weight and repository-local model-cache patterns. No tracked public synthetic fixture became ignored.

## 19. Dockerignore

PASS for the current frozen build context. It excludes .git/.github, .env, virtual environments, Python caches/logs, runtime/private data, results, screenshots, tests, offline evaluation, frontend caches/build, PDFs and local database/model artifacts.

The file is unchanged because it is a hashed V3.83 deployment input. If a new repo-local model-cache convention is introduced, its pattern must be added in a future packaging phase followed by image revalidation.

## 20. Metrics Consistency

PASS across the public package docs: V377 remains 54/69, 78.26%, 9 FA, 3 FR, AR 76.92%, AbR 79.07%, hard negative 10/10; native soak remains 11,678 requests/0 errors; protected hashes remain 28/28.

All accuracy numbers are tied to the frozen aligned benchmark, not presented as universal industrial accuracy.

## 21. Research Claims

PASS. NO-GO, PARTIAL, NOT_JUSTIFIED, experimental/default-OFF and DEFERRED results remain research boundaries, not product capability claims. Layout-aware ingestion remains a separate future project.

## 22. Provider Claims

PASS. Public docs retain OPTIONAL_PROVIDER_CAPABILITY_NOT_VALIDATED. Core retrieval/Evidence/demo/deployment validation is keyless; real LLM generation is not claimed as certified.

## 23. CI Claims

PASS. Current claim remains CI_CONFIGURED / CI_EXTERNAL_EXECUTION_PENDING. No document claims remote CI PASS.

## 24. Publication Plan

PUBLICATION_PLAN.md now records LICENSE=MIT, LICENSE_DECISION=RESOLVED, the repository name/description, CI requirements, future authorization sequence and explicit non-actions.

## 25. Repository Metadata

Proposed name: industrial-knowledge-rag.

Proposed description: “Deployment-validated industrial knowledge RAG with hybrid retrieval, model-aware evidence checks, safe abstention, structured evaluation, and Docker/FastAPI operations.”

Default branch: main. Public visibility is prepared but still requires explicit external-write authorization.

## 26. Topics

Suggested only, not set: rag, industrial-ai, fastapi, retrieval, hybrid-search, llm, observability, docker, evaluation, safe-ai.

## 27. Release Notes Draft

RELEASE_NOTES_V3.84_RC.md is updated to record the MIT License while retaining the external-write and exact-SHA CI prerequisites.

## 28. Version Candidate

Candidates: rc-v3.84 or v0.1.0-rc1. No version is selected and no tag exists. Final version is OWNER DECISION.

## 29. Correctness Freeze

Canonical digest remains dcd548906ef2b1f233282283b8684f8c82066dc5eea9f0179ff45a4034a5aa1d with 54/69 correct, 9 FA, 3 FR and hard negative 10/10. The private benchmark was not rerun because V3.85 changes only ignore/publication documentation.

## 30. Protected Hashes

backend.v382_release_guard: 28/28 PASS, no drift/missing/unpinned entry. RAG runtime semantic diff is zero.

## 31. Private Safety

TRACKED_PRIVATE_FILES=0. HISTORY_PRIVATE_FILES=0. Current and reused history secret scans pass. The one local provider credential remains ignored, unprinted and absent from all Git objects.

## 32. PUBLIC_REPOSITORY_PRECHECK_READY

**TRUE.**

RC manifests, MIT license, private/secret/corpus guards, README/links, current absolute-path scan, public demo, CI/Docker audits, metrics, correctness and protected hashes all pass.

## 33. External Authorization Boundary

Reached. Status is **AWAITING_OWNER_EXTERNAL_WRITE_AUTHORIZATION**.

No gh repo create, git remote add, git push, git tag or gh release command was run.

## 34. Remaining Blockers

Remaining internal pre-publication blockers: **NONE**.

Non-blocking disclosed notes: five historical non-secret repository-path blobs; optional immutable action-SHA pinning; container performance investigation; provider capability and remote CI remain external-pending.

## 35. NEXT_PHASE

**AWAITING_OWNER_EXTERNAL_WRITE_AUTHORIZATION**

Repository creation, remote setup and push require explicit future authorization. Tag and release require a separate later authorization after exact-SHA CI evidence.
