# V3.73 Identity Metadata Lineage & Enrichment Report

## Status

**IDENTITY_METADATA_ENRICHMENT_PARTIAL**

Metadata enrichment implemented and verified (chunks carry manufacturer/product_series/equipment_model), but aggregate metrics unchanged: 48 correct / 69.6% accuracy.

## Root Cause Chain

1. PyPDFLoader → only source/page metadata (CASE B confirmed)
2. DocumentIdentityResolver added: filename-based extraction works
3. Chunks enriched with identity fields via rag_core.load_pdf()
4. Re-indexed: 2448 docs → 7333 chunks with metadata
5. BUT: Evidence runtime still rejects IDENTITY queries

## Remaining Investigation Needed

Evidence's `identity_from_metadata()` may use different canonical key names,
or requires family-level info we're not populating. Further debugging needed
to align metadata schema between ingestion and Evidence consumer.

## Safety Preserved

All safety metrics unchanged: HARD_NEGATIVE 10/10, OOD FA=2, no new unsafe acceptances.
