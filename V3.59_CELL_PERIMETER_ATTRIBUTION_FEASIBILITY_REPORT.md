# V3.59 Cell Perimeter Attribution Feasibility Report

## 1. Status

**CELL_PERIMETER_FEASIBILITY_FAILED**

**STRUCTURED_TABLE_PRODUCER_DIRECTION = FROZEN_PARTIAL**

## 2. Git

- before: `d1ba401`
- after: 本轮提交(见下文 Git 节)

## 3. V3.58 Preservation

V3.58 状态保持 NOT_READY 不变。所有历史结论、代码和指标不受影响。

## 4–7. Architecture Decision & Go/No-Go Audit

### Topology Taxonomy(52 个可定位 false merge)

| Primary Cause | Count |
|---|---|
| SHARED_EDGE_AMBIGUITY(v-divider 可证分离) | 19 |
| CROSS_COLUMN_RECTANGLE | 19 |
| CROSS_ROW_RECTANGLE(h-divider 可证) | 14 |
| SIGNAL_INSUFFICIENT(borderless stacked/wrapped) | 8 |

### Resolvability

| 判定 | Count |
|---|---|
| TOPOLOGY_RESOLVABLE(interior divider / region gap / horizontal gap) | **44 (84.6%)** |
| SIGNAL_INSUFFICIENT | 8 (15.4%) |

**GO 决定**:84.6% ≥50% → 授权实现完整 perimeter algorithm。

## 8–14. Implementation

`backend/retrieval/table_cell_perimeter_v359.py`:
- StructuralEdge(edge_id/orientation/x0/y0/x1/y1/page/source_type/confidence)
- build_structural_edges_from_pdfplumber(page, page_number)
- boundary_positions(edges, orientation, span, coverage_min, tolerance)
- build_lattice(atoms, edges) → h_bounds/v_bounds
- reconstruct_perimeter_cells(document_id, page_index, atoms, edges)

Interior-divider veto 和 ambiguity abstention 已实现但效果有限。

## 15–24. 结果(P59 vs baselines)

| 指标 | F0 layout | F1/F2 v2 | R4_v3_rect | **P59_perimeter** |
|---|---|---|---|---|
| Exact cell resolution | 73.1% | 73.9% | 85.7% | **20.7%** |
| False merge rate | 5.6% | 13.0% | — | **13.2%** |
| Parameter localization | 73.6% | 96.5% | 89.9% | **81.2%** |

P59 在所有指标上均不如 V3.58 v3 rectangle-first 方法。

原因:lattice boundary 从 ruling edges 推导,但真实工业表格的 ruling
edges 与文本行/列的几何对齐存在系统性偏差(浮点误差、行距变化、
部分栅格),导致 lattice cells 无法正确包含对应的 text atoms。

## 25–26. Performance & Complexity

Perimeter 模块单页处理 <0.1s;复杂度 O(E×A) 其中 E=edge 数、A=atom 数,
无组合爆炸风险。

## 27–29. Safety / Regression / Hashes

V3.51/V3.54 哈希不变;后端 806 passed;前端构建通过。

## 30–33. Engineering Decision & NEXT_PHASE

### 结论

结构化 table producer 方向在当前工具链(pypdf + pdfplumber)下已达
信号天花板。三种 row-boundary 信号均已系统测试:

| 信号 | 版本 | 结果 |
|---|---|---|
| layout text heuristics | V3.57 R1-R3 | recall 24%, precision 100% |
| ruling fusion(row boundary) | V3.57 R4 | 零改善 |
| perimeter attribution(lattice cells) | V3.59 | recall 21%, FM 13% |

### 根因(事实)

工业手册中 wrapped-cell 与 stacked-cell 的视觉特征完全一致
(相同列带、下一行距、无分隔线),仅凭公开 PDF 几何信号无法区分。
解决此问题需要以下三者之一:
(a) 字符级解码坐标 + ToUnicode CMap(PyMuPDF 提供,AGPL);
(b) 更强的 layout parser(pdfplumber 升级或自定义);
(c) 改变 Producer/Evidence 契约以接受 PARTIAL cell(降低 precision 要求)。

以上均为架构决策,不在 refinement 预算内。

### 建议(§53 responsibility recommendation)

A. **保留 table-region metadata**:Producer 仅输出"此区域是表格"
   及行列带信息,不生成 cell ownership proof。Evidence 层可使用
   region 信息辅助检索但不做精确绑定。
B. **仅对 fully-ruled tables 启用 structured proof**:约 40% 的
   benchmark 表格具有完整栅格,这些表格的 cell 分辨率可达 90%+
   且 false merge ≈0%(R4 审计确认)。需要 PyMuPDF 或等价依赖。
C. **回到 Evidence architecture**:保持 FLAT_TEXT_OWNERSHIP_INFERENCE
   forbidden;将 table structure 问题标记为 known limitation 并
   通过 Evidence 层的保守弃权策略补偿。

以上三条路线由项目 owner 决策。本报告为 Structured Table 方向的
最终 feasibility 结论。
