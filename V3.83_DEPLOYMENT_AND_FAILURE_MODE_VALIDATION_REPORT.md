# V3.83 Deployment & Failure-Mode Validation Report

Date: 2026-08-27 (Asia/Shanghai)

## 1. Status

`DEPLOYMENT_FAILURE_MODE_VALIDATION_PARTIAL`

The production-like native process path passed lifecycle, recovery, observability, upload/queue, load, soak, persistence, frontend connectivity, correctness, and privacy validation. The repository's formal release topology is Docker Compose, but the local Docker daemon was unavailable; image build and container lifecycle were therefore not executed. A real successful LLM generation was also unavailable, so the `ANSWER` metric branch remains unexecuted.

## 2. Git

- Recovery branch: `main`
- Recovery HEAD: `bde6f169620e8e5305ed601bf4e16a87aebcb443`
- Recovery worktree: clean
- Operational fix: `93da820` (`fix: harden readiness dependency failure contract`)
- Validation tooling/freeze: `daaecc0` (`test: validate V3.83 deployment failure modes`)
- Documentation/report: the immediately following commit containing this report
- Push: **NO**

## 3. V3.82 Preservation

V3.82 was recovered exactly at the expected HEAD. No Evidence, EvidencePolicy, threshold, Support, NLI, requirement extraction, retrieval, score-lineage, identity, OOD, table, generation, evaluator, adapter, or benchmark-gold semantics changed. The final V3.82 research guard remains 28/28 PASS.

## 4. RC Freeze Manifest

`V383_DEPLOYMENT_VALIDATION_MANIFEST_V1` records source HEAD, deployment/runtime artifacts, 28 protected runtime hashes, private benchmark hashes, dependency versions, frontend build hash, and the frozen index logical digest. The one validation-discovered operational overlay is explicitly pinned: `backend/main.py`, its regression test, and the V3.83 validator.

## 5. Initial Baseline Replay

`V377_ALIGNED_BENCHMARK_V2` ran twice before deployment validation:

| Total | Correct | FA | FR | Accuracy | AR | AbR | HN | Digest |
|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 69 | 54 | 9 | 3 | 78.26% | 76.92% | 79.07% | 10/10 | `dcd548906ef2b1f233282283b8684f8c82066dc5eea9f0179ff45a4034a5aa1d` |

Score-lineage fidelity was 69/69; both run digests were identical.

## 6. Deployment Topology

The formal topology in `DEPLOYMENT.md` is a Docker Compose light-mode FastAPI backend on port 8000, persistent named volumes, and a separately built/hosted frontend. Full mode is a derived deployment requiring full dependencies, a compatible Chroma index, and model cache. The executed fallback was the documented native Uvicorn production-like process in light mode with a synthetic public index.

## 7. Docker Runtime Availability

| Gate | Result |
|---|---|
| Docker client | 29.6.2 available |
| Compose | 5.3.1 available |
| `docker compose config --quiet` | PASS |
| Daemon | unavailable (`dockerDesktopLinuxEngine` pipe absent) |
| Image build | BLOCKED / NOT EXECUTED |
| Container startup/lifecycle | BLOCKED / NOT EXECUTED |

`.dockerignore` excludes `.env`, private PDFs/benchmarks/results, indexes/vector DBs, `.git`, caches, and frontend artifacts. Image-content inspection could not be executed without an image.

## 8. CI Reality Check

`.github/workflows/ci.yml` is syntactically present and configures public backend, offline evaluation, full-dependency import, frontend test/build, Compose validation, and image build. No Git remote is configured and no V3.83 commit was pushed. Status: `CI_CONFIGURED / CI_EXECUTED_NOT_EXECUTED`; CI PASS is not claimed.

## 9. Clean Startup

Fresh synthetic runtime state was created only for the validator. The documented production command started real Uvicorn processes with production env, debug OFF, JSON logging, light/hybrid mode, and memory queue/limiter. Formal-run first liveness was observed at 1080.31 ms and readiness at 1104.32 ms.

## 10. Liveness Lifecycle

`/live` stayed HTTP 200 through optional-provider absence and required-index failure. This confirms process-only liveness. Connection refusal before startup and after shutdown confirmed the externally observable STARTING/STOPPED boundaries.

## 11. Readiness Lifecycle

The observed supported lifecycle was:

`STARTING -> LIVE -> READY(degraded optional provider) -> UNAVAILABLE(required index missing/invalid) -> READY(degraded) -> SHUTTING_DOWN -> STOPPED`.

Missing or invalid indexes returned 503; restoring the valid index returned 200 without process restart. The project does not expose a separate state-machine endpoint, so unsupported state names were not fabricated.

## 12. Restart Validation

Three formal clean restart cycles all completed startup, readiness, smoke, graceful shutdown, complete shutdown logs, and port release: **3/3 (100%)**. Shutdown durations were 220.92, 201.20, and 208.43 ms. Windows `CTRL_BREAK_EVENT` produced process exit code 3, but no terminate fallback was used and Uvicorn logged application shutdown complete and finished server process. No orphan process or port leak remained.

## 13. Index Persistence

- Deployed synthetic light-index SHA256 start/end: `b83bb64ea9c8bb79a3f5060b0125d746a62c02ad0216c92177cff8dd60366b83` / identical.
- Frozen private Chroma logical content: 7333 chunks; canonical digest start/end `644c9dc8f835a48cb2ea5399b8a17eb042d9dac4c71a6a55f3bf147d52d3700b` / identical.

Chroma changes `chroma.sqlite3` bookkeeping bytes on a read-only open, so raw physical SQLite hashing is not a stable content identity. The gate therefore uses canonical IDs/documents/metadata/embeddings plus the frozen corpus/benchmark hashes. Logical content, query results, and canonical replay digest stayed unchanged.

## 14. Abrupt Termination

A synthetic-only service process received bounded abrupt termination. It exited in 215.18 ms with port release, the synthetic index digest remained identical, and the next clean process returned live/ready successfully. No private index was subjected to destructive injection.

## 15. Required Dependency Failure

The selected light topology's required runtime dependency is its JSON index. Removing it during runtime produced `/ready` 503 with `DEPENDENCY_UNAVAILABLE`, safe details, request ID, and `retryable=true`; `/live` remained 200. Dependency-failure metrics increased.

## 16. Optional Dependency Failure

With both LLM providers unavailable, `/live` remained 200 and `/ready` remained routable HTTP 200 with status `degraded` and `llm_provider=no_provider_configured`. Core retrieval and safe ABSTAIN remained serviceable.

## 17. Dependency Recovery

Required-index recovery passed dynamically: 200 -> 503 -> 200 for both missing and malformed synthetic indexes. Optional provider configuration is read at process startup; it requires a restart. No runtime-parseable provider credential was available, so real provider restoration was not claimed.

## 18. Index Failure / Recovery

Actual release-mode light-index missing and invalid-JSON paths were exercised against a real HTTP process. Both blocked readiness and both recovered deterministically after restoring the known-good bytes. Full-mode manifest/model/dimension/schema mismatch remains covered by focused tests, not represented as a light-mode E2E result.

## 19. Provider Failure

A real request reached the configured Groq path without a usable key and returned a safe HTTP 400 operational error contract. No secret, traceback, private path, or provider internals leaked. Status: `REAL_PROVIDER_UNAVAILABLE`; no fake key or fake success was used.

## 20. Request ID E2E

`req-v383-e2e-trace-00000001` matched the response header and structured `ask_outcome`/request-completion logs. Invalid-request error bodies matched their response request IDs.

## 21. Concurrent Trace Isolation

Eight concurrent requests used eight unique caller IDs. All response IDs were unique and preserved, and every ID appeared in the corresponding structured logs. No cross-request trace contamination was found.

## 22. Metrics E2E

HTTP success/error, latency, dependency failure, queue gauges, retrieval/Evidence latency, and ABSTAIN counters grew under real requests. `ANSWER` could not be observed because no real provider was available; therefore metrics E2E is **PARTIAL**, not falsely PASS. Process-local metric reset on restart matched the documented design.

## 23. Logging Privacy E2E

Synthetic secret, private-query, and private-path sentinels were sent through validation-error traffic. They appeared in neither service logs nor metrics. Structured logging retained safe request IDs and outcomes only.

## 24. Error Contract E2E

- Invalid request: HTTP 422 / `VALIDATION_ERROR` / request ID / non-retryable.
- Missing or invalid index readiness: HTTP 503 / `DEPENDENCY_UNAVAILABLE` / request ID / retryable.
- Provider unavailable: safe HTTP 400 contract with request ID.
- Invalid upload: HTTP 400 / `INVALID_REQUEST`.

No response exposed traceback, raw sensitive input, secret, or private path.

## 25. Upload Lifecycle

A generated synthetic PDF passed upload validation, safe filename handling, processing, and draft-index creation. Repeating the same idempotency key returned the same job; a non-PDF payload was rejected safely. Synthetic draft files were removed after validation.

## 26. Queue Lifecycle

The formal Compose default uses the memory queue. A real upload was enqueued, executed by the in-process worker, and reached `succeeded`; duplicate/idempotent behavior passed. Worker cleanup was exercised on every graceful shutdown. Redis/RQ was not selected and was not started merely for checklist coverage.

## 27. Graceful Shutdown

Four graceful shutdowns (three formal restarts plus post-soak recovery) completed Uvicorn lifespan shutdown, queue close, port release, and index preservation in 201–221 ms for the three formal cycles and 204.34 ms after soak. No zombie/orphan remained.

## 28. Load Validation

Real HTTP process, 24 requests per level:

| Concurrency | Success | Throughput | Median | p95 |
|---:|---:|---:|---:|---:|
| 1 | 24/24 | 786.96 rps | 1.02 ms | 1.96 ms |
| 2 | 24/24 | 673.59 rps | 0.98 ms | 18.38 ms |
| 4 | 24/24 | 541.08 rps | 2.79 ms | 18.66 ms |
| 8 | 24/24 | 603.13 rps | 4.89 ms | 25.62 ms |

## 29. Soak Test

The bounded soak ran 600.12 seconds at concurrency 4 with 11,678 requests: 2,803 live, 2,920 ready, 2,803 health, 2,919 metrics, and 233 safe ABSTAIN calls. Success was 100%; unexplained 5xx, crashes, deadlocks, index mutation, and trace contamination were zero.

## 30. Latency

Soak latency was median 2.47 ms, p95 20.66 ms, max 44.85 ms. This includes real HTTP and endpoint-mix overhead and is not the same method as V3.82's direct retrieval+Evidence p95 5.64 ms, so the 2x investigation rule is not applied across incompatible measurements. No catastrophic deployment-layer regression was observed.

## 31. Resource Stability

Soak RSS was 145.89 MB start, 150.58 MB end, and 156.21 MB peak (+4.69 MB end-to-start). CPU time was 19.906 seconds. Handles were 459 start, 434 end, and 471 peak. Neither memory runaway nor handle leak criteria triggered.

## 32. Operational Runbook Validation

The runbook's native startup, `/live`, `/ready`, `/health`, `/metrics`, failure diagnosis, restart, upload/job, and Ctrl+C shutdown paths were executed. Compose config validation worked; container start/stop commands were not claimed because the daemon was unavailable. The V3.83 operator-invoked soak command and readiness error contract are now documented.

## 33. Deployment Documentation Validation

Ports, runtime modes, volumes, health/readiness semantics, production env, memory/Redis distinctions, frontend origin, and private build-context exclusions matched the repository. Docs now record the validated native lifecycle and the still-blocked container boundary rather than claiming an unexecuted topology.

## 34. Release Guards

Public smoke PASS, offline evaluation quality gates PASS, Python compile PASS, Compose config PASS, research/private release guard PASS, and frontend-to-backend CORS connectivity PASS.

## 35. Final Baseline Replay

After all deployment validation and the operational fix, `V377_ALIGNED_BENCHMARK_V2` again ran twice with 69/69 formal rows, 54 correct, 9 FA, 3 FR, 78.26% accuracy, 76.92% AR, 79.07% AbR, HN 10/10, score lineage 69/69, and the identical canonical digest `dcd548906ef2b1f233282283b8684f8c82066dc5eea9f0179ff45a4034a5aa1d` on both runs.

## 36. Protected Hashes

V3.82 research freeze: **28/28 PASS**, no drift/missing/unpinned file. RAG semantic diff: **ZERO**. The V3.83 operational overlay is separately pinned in the validation manifest.

## 37. Backend Tests

`python -m pytest backend -v`: **948 passed, 2 skipped, 0 failed**, plus 5 subtests passed. Focused readiness regression: PASS.

## 38. Frontend

Vitest: **34/34 PASS**. Production build: PASS; dist tree hash remained `dc5103485ad71f08d770228482cc140214b297bdc70fef54a4e57e0bbc707683`. Real Vite preview returned 200 and API CORS preflight returned 200 with the exact configured origin.

## 39. Private Safety

`TRACKED_PRIVATE_FILES = 0`. Private PDFs, aligned benchmark payloads, vector DB, results, logs, `.env`, secrets, and synthetic runtime artifacts remain untracked/ignored. AutoCourse-RAG remained read-only at `a430235d28de0fe79c0790acc2c99220fc239d1b`.

## 40. Operational Fixes

Validation exposed one production defect: readiness 503 responses lacked the common safe error fields, and readiness failures never incremented `dependency_failures_total`. The minimal fix adds `DEPENDENCY_UNAVAILABLE` fields and bounded dependency labels, with a focused regression. No RAG code or correctness semantics changed.

## 41. Remaining Blockers

1. `CONTAINER_RUNTIME_VALIDATION_BLOCKED`: Docker daemon unavailable; image build, image-content inspection, container startup, container health/readiness, restart/shutdown, and volume persistence are unexecuted.
2. `REAL_PROVIDER_UNAVAILABLE`: successful generation and the real `ANSWER` metric branch are unexecuted.
3. `CI_EXECUTED_NOT_EXECUTED`: workflow configured, no remote/push/history for these commits. This is reported but is not used as a fake local failure gate.

## 42. Engineering Decision

The native deployment lifecycle is validated, but the formal Docker Compose release topology is not. Per the V3.83 state machine, the honest status is `DEPLOYMENT_FAILURE_MODE_VALIDATION_PARTIAL`. V3.84 packaging must not begin yet.

## 43. Release Candidate Manifest

`V383_RELEASE_CANDIDATE_MANIFEST` was **not generated** because READY criteria were not met. The pre-validation `V383_DEPLOYMENT_VALIDATION_MANIFEST.json` remains the authoritative frozen target plus explicitly hashed operational overlay.

## 44. NEXT_PHASE

Resolve the deployment runtime environment, then rerun only the blocked V3.83 gates: real image build/content audit, Compose container startup, health/readiness, restart/shutdown, and volume/index persistence. With an authorized provider credential, also close the successful `ANSWER` metrics branch. Do not enter feature development or Accuracy Research.
