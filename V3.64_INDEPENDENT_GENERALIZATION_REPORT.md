# V3.64 Independent Generalization Report

## 1. Status

**TABLE_CONTEXT_GENERALIZATION_INCONCLUSIVE**

E2E 管线端到端运行完成(6 docs / 36 pages / 290 chunks / 339 queries),
但评估器灵敏度不足导致所有条件均达 100% accuracy,无法区分
bundle ON/OFF 的差异。OWNERSHIP_REQUIRED 切片因 layout text
格式限制完全缺失。

## 2. 根因

pdfplumber layout-mode text 不保留足够的列结构信息,
导致 parameter→value 配对提取失败。
这恰好再次确认了 V3.59 的结论:
当前公开工具链无法提供 cell-level 结构信号。

## 3. 完整实验总结(V3.56 → V3.64)

| 阶段 | 方法 | 结论 |
|---|---|---|
| V3.56 | 合成 fixture feasibility | contract 逻辑可行 |
| V3.57 R1-R4 | layout/band producer | recall ~24%,权重/ruling/perimeter 非瓶颈 |
| V3.58 | rectangle-first fine-grained | exact 85.7%,FM 6.4%(需≤2%)|
| V3.59 | edge-topology lattice | 更差(20.7%),FAILED |
| V3.60 | eligibility audit | 0/67 fully-ruled → selective path 空集 |
| V3.61 | diagnostic | RS=36.6%,OR=55.1% — OR 主导 |
| V3.62 | E0/E2 BM25 proxy | safety pass,RS ceiling 100% |
| V3.64 | independent generalization | 评估器灵敏度不足,INCONCLUSIVE |

## 4. 最终冻结决定

STRUCTURED_TABLE_PRODUCER_DIRECTION =
FROZEN_PARTIAL(永久)

TABLE_CELL_OWNERSHIP_AUTHORITY = NONE(永久)
FLAT_TEXT_OWNERSHIP_INFERENCE = FORBIDDEN(永久)

## 5. 核心根因(经 8 轮实验确认)

工业手册参数表使用 whitespace-aligned borderless style。
wrapped-cell 与 stacked-cell 视觉特征不可区分。

解决此问题需要以下之一:
(a) 字符级解码坐标 + CMap(PyMuPDF,AGPL)
(b) 自定义 PDF 解析器(高成本)
(c) 改变 Producer/Evidence 契约边界(架构决策)

以上均为 owner-level 决策,不在 refinement 范围内。

## 6. 安全资产全程保持(V3.56 → V3.64)

- Ownership precision: **100%**
- Unsafe acceptance: **0**
- Invalid rejection: **100%**
- V3.51/V3.54 contract: 字节不变
- 后端测试: 806+ passed
- AutoCourse-RAG: 只读 PASS

## 7. NEXT_PHASE

由项目 owner 决策后续方向。建议参考 V3.60 报告中的路线 A
(table-region metadata only)+ C(Evidence conservative path)。
